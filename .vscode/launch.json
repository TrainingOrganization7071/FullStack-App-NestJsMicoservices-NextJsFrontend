{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/services/microone/package.json",
                "localRoot": "${workspaceFolder}/services/microone"
            }
        }
    ]
}